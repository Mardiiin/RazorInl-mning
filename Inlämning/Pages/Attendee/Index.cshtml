@page
@model Inlämning.Pages.Attendee.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Attendee[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendee[0].Emailaddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendee[0].PhoneNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Attendee) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Emailaddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AttendeeID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AttendeeID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AttendeeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
